<nav class="bg-white shadow rounded mb-4 flex flex-wrap text-sm overflow-hidden">
    <a href="{{ path('app_lastfm_unmatched') }}"
       class="px-4 py-2 border-r {{ current_tab == 'tracks' ? 'bg-slate-900 text-white' : 'text-slate-700 hover:bg-slate-100' }}">
        Par titre
    </a>
    <a href="{{ path('app_lastfm_unmatched_artists') }}"
       class="px-4 py-2 border-r {{ current_tab == 'artists' ? 'bg-slate-900 text-white' : 'text-slate-700 hover:bg-slate-100' }}">
        Par artiste
    </a>
    <a href="{{ path('app_lastfm_unmatched_albums') }}"
       class="px-4 py-2 {{ current_tab == 'albums' ? 'bg-slate-900 text-white' : 'text-slate-700 hover:bg-slate-100' }}">
        Par album
    </a>
</nav>
